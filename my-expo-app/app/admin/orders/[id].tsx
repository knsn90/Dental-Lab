// app/admin/orders/[id].tsx
// Legacy admin route — yeni OrderDetailScreen'e re-export.
// Tüm hero / ticket / popup / responsive scale değişiklikleri buradan da
// erişilebilir.
export { OrderDetailScreen as default } from '../../../modules/orders/screens/OrderDetailScreen';
