/**
 * MobileViewer3D web stub — bu dosya sadece bundle'a girer ve `require('./MobileViewer3D')`
 * web tarafında bu null component'i çağırır. Native'de `.native.tsx` resolve edilir.
 *
 * Böylece web bundle'a `react-native-webview` ve viewer HTML template girmez.
 */
export default function MobileViewer3DWebStub(): null {
  return null;
}
