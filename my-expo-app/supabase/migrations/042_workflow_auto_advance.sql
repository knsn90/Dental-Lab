-- ─────────────────────────────────────────────────────────────────────────────
-- 042 — (DEPRECATED / NO-OP)
-- Migration 039_workflow_engine zaten advance_workflow_stage stored
-- procedure'ünde son stage tamamlandığında work_orders.status'u
-- otomatik 'kalite_kontrol' yapıyor. Ekstra trigger gereksizdi.
-- Bu dosya tarihsel sıralamayı korumak için boş bırakıldı.
-- ─────────────────────────────────────────────────────────────────────────────

SELECT 1;
